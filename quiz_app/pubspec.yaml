name: quiz_app
description: "AI-powered Quiz App with Gemini and Flask."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0  # Set to a stable range compatible with modern packages

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Core Networking & Storage
  http: ^1.2.0
  shared_preferences: ^2.2.2
  
  # File Picker for Web/Mobile (Updated version)
  file_picker: ^8.0.0
  
  # Web-compatible PDF handling (Replaces conflicting pdf_text)
  syncfusion_flutter_pdf: ^22.1.54

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Standard lints for your SDK version

flutter:
  uses-material-design: true

  # Ensure you register assets if you use them:
  # assets:
  #   - assets/